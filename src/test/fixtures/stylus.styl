$nprogressColor ?= $accentColor

#nprogress
  pointer-events none
  .bar
    background $nprogressColor
    position fixed
    z-index 1031

// mix non-indent grammer
.app { color: red; }

// raw css
@css {
  .main {
    color: blue;
    height: .5rem;
    width: 0.8rem;
    color: rgba(1,1,0, 0.34);
  }
}
